# 项目整理记录

日期：2026-09-06。

## 整理范围与方案

沿用现有 C++ 分层目录，以根目录 README 为运行入口、`docs/README.md` 为课程资料入口。此次是文件归档和文档维护，不涉及产品行为或架构变更。

仅添加目录说明无法解决旧程序混放和命令歧义；大规模移动源码及历史报告会增加路径变更成本。因此采用现有结构内整理，历史证据保持原路径和内容。

## 执行清单

- [x] 将根目录五个旧 EXE 移入 `build/legacy-root/`，保存逐文件校验清单。
- [x] README 分别列出 MSVC 和 GCC 的构建、测试、运行命令，避免混用生成器。
- [x] 添加文档导航、测试导航和交付清单。
- [x] 标注原始计划与当前执行状态的区别，修正测试矩阵的过度概括。
- [x] 检查文档链接、归档文件一致性及根目录状态。

验证结果：41 个本地 Markdown 链接存在，根目录无 EXE，归档五个文件的 SHA256 与移动前一致。校验清单位于 `build/legacy-root/manifest.csv`。既有构建目录、客户端和报告证据保留；未删除缓存、修改系统设置或创建 Git 提交。

## 验证边界

本轮只维护文档及归档旧构建产物，不重复宣称新增功能测试通过。产品验收依据仍为 [正式测试报告](reports/2026-09-06-test-report.md)。源码、公共接口、CMake 和测试断言保持现状；课程签署和独立复现仍待完成。
